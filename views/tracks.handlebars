<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{title}}</p>
  <p>{{artist}}</p>
  <p>{{filename}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <button class="btn btn-primary">Like this track</button>
  </p>
</div>
